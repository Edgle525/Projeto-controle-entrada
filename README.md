A parte do backend preparando o ambiente está praticamente feita. A conexão com o banco de dados está funcionando perfeitamente. 
Agora só precisamos montar a parte física e sincronizar com o restante do projeto. Isso também envolve as regras de negócio.
